<Window x:Class="APLICACION.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Juego de memoria" Height="500" Width="500">
    <Grid>
        <Grid.RowDefinitions>
        <RowDefinition Height ="3*"/>
        <RowDefinition Height ="*"/>
        </Grid.RowDefinitions>
        <Grid x:Name= "pantalladejuego">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width ="*" />
                <ColumnDefinition Width ="*"/>
            <ColumnDefinition Width ="*" />
            <ColumnDefinition Width ="*"/>
            </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
    </Grid>
        <StackPanel Grid.Row="1">
            <TextBlock Text="Aciertos"/>
            <TextBox x:Name="aciertos" IsEnabled="False"></TextBox>
            <TextBlock Text="fallido"/>
            <TextBox x:Name="fallidos" IsEnabled="False"></TextBox>
            <TextBlock Text="tiempo"/>
            <TextBox x:Name="tiempo" IsEnabled="False"></TextBox>
        </StackPanel>
    </Grid>
</Window>
